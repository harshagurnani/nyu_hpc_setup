Build docker image for my HPC projects at NYU, stored on Dockerhub as harshagurnani/nyu_hpc_py310

Once your docker image is built successfully, you can trying building and deploying the singularity container; for torch examples see [run_cputorch.sh](run_cputorch.sh) and [build_torch.sh](build_torch.sh).
